input { 
syslog {
  type => syslog
  port => 5514
 }
stdin { }
lumberjack {
  port => 6782 ##need to create
  ssl_certificate => "/logstash/server.crt" ##need to create
  ssl_key => "logstash/server.key" ##need to create
  type => "lumberjack"
  }
}
output {
  elasticsearch { host => localhost }
  stdout { codec => rubydebug }
}
